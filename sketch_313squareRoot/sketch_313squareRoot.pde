//Given a number, `square_root` returns the square root of the given number

float squareRoot(float givenNumber) {
  return sqrt(givenNumber);
}

void setup() {
  println(squareRoot(64)); //8.0
  println(squareRoot(529)); //23.0
}
